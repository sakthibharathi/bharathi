#include <stdio.h>
int main()
{
    int n, k, sum = 0;
    
    printf("Enter a positive integer: ");
    scanf("%d",&n);

    for(k=1; k <= n; ++k)
    {
        sum += i;   
    }

    printf("Sum = %d",sum);

    return 0;
}

