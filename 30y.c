#include <stdio.h>
 void main()
{
    int k, n, s = 0;
 
    printf("Enter an integer number \n");
    scanf ("%d", &n);
    for (k = 1; k <= n; k++)
    {
        s = s + i;
    }
    printf ("Sum of first %d natural numbers = %d\n", n, s);
    return 0;
}
